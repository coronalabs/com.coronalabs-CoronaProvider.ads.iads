Sources for the plugin `CoronaProvider.ads.iads`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.ads.iads" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
